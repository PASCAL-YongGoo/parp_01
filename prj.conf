# Application-specific Configuration
# (Board-level hardware configs are in nucleo_h723zg_parp01_defconfig)

# ========================================
# USB Device Stack (New API)
# ========================================
CONFIG_USB_DEVICE_STACK_NEXT=y
CONFIG_USBD_LOG_LEVEL_INF=y

# ========================================
# USB CDC ACM (Virtual Serial)
# ========================================
CONFIG_USBD_CDC_ACM_CLASS=y
CONFIG_UART_LINE_CTRL=y
CONFIG_SERIAL=y

# Console output via CDC ACM
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y

# ========================================
# USB HID Keyboard
# ========================================
CONFIG_USBD_HID_SUPPORT=y

# ========================================
# Logging (CDC ACM output)
# ========================================
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=3
CONFIG_LOG_MODE_DEFERRED=y

# ========================================
# Shell (CDC ACM input)
# ========================================
CONFIG_SHELL=y
CONFIG_SHELL_BACKEND_SERIAL=y
CONFIG_SHELL_START_OBSCURED=y
CONFIG_SHELL_CMDS_SELECT=y

# ========================================
# GPIO (for buttons and LEDs)
# ========================================
CONFIG_GPIO=y

# ========================================
# I2C and EEPROM (for password storage)
# ========================================
CONFIG_I2C=y
CONFIG_EEPROM=y
CONFIG_EEPROM_AT24=y
